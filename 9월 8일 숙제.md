# 문제 1 코드
```
from itertools import permutations

cards = [0,1,2,3,4,5]
num_lits = list(permutations(cards, 4))

su = []
for i in num_lits:
    q = 1000 * i[0] + 100 * i[1] + 10 * i[2] + i[3]

    if q % 6 ==0 and q >= 1000:
        su.append(q)

print(min(su))
```
# 문제 2 코드
```
from itertools import combinations

pocket = [10, 20, 33, 40, 55, 61, 78, 80, 91, 100]
num_lits = list(combinations(pocket, 3))
mi = 150
g=0
for i in num_lits:
    su = sum(i)
    if su <= 150:
        g += 1
        if mi > 150 - su:
            mi = 150 - su

if g == 0:
    print("불가능")
else:
    print(150 - mi)
```
# 완전 탐색
- **정의: 가능한 모든 경우의 수를 빠짐없이 전부 확인하여 정답을 찾는 가장 단순하고 확실한 방법**
## 개념과 특징
- 전수 조사: 컴퓨터의 빠른 계산 속도를 믿고 모든 가능성을 시도합니다.
- 장점: 예외 없이 정확한 정답을 찾을 수 있고, 이해하거나 코드로 구현하기 쉽습니다.
- 단점: 경우의 수가 너무 많으면 실행 시간이 지나치게 오래 걸리거나 메모리가 부족해질 수 있습니다.
## 주요 종류
- 백트래킹 (Backtracking): 탐색 도중 답이 아니라고 판단되면 되돌아가서 다른 경로를 확인합니다.
- 순열과 조합: 파이썬의 itertools 라이브러리 등을 활용해 순서를 고려하거나 무작위로 그룹을 묶는 경우를 쉽게 만듭니다.
# 1. 백드래킹
- **개념: 모든 경우의 수를 전부 고려하는 알고리즘이다.**
- 복잡한 고려 없이 쉽게 짤 수 있다는 것이 장점이다.
- 상태공간을 트리로 나타낼 수 있을 때 적합한 방식이다.
- 일종의 트리 탐색 알고리즘이라고 봐도 된다.
### <방식에 따라서 3가지로 나뉜다.>
1. 깊이우선탐색(Depth First Search, DFS)
2. 너비우선탐색(Breadth First Search, BFS)
3. 최선 우선 탐색(Best First Search/Heuristic Search)이 있다.
# 2. 순열
- **정의: 서로 다른 n개 중에서 r개를 택해 순서를 고려하여 일렬로 나열하는 경우의 수 또는 그 방법**
### <순열의 핵심 개념순서의 중요성>
- 순열은 뽑는 순서가 다르면 다른 경우로 봅니다 (예: A, B를 뽑아 줄을 세울 때 AB와 BA는 다른 경우).
- 기호 표기: 서로 다른 n개 중 r개를 택하는 순열은 \({}_{n}P_{r}\) 또는 P(n, r)로 나타냅니다.
- 계산 공식:\(_nP_r = \frac{n!}{(n-r)!}\) (n부터 시작해 1씩 줄이며 r개의 숫자를 곱함)
# 3. 조합
- **정의: 순서와 상관없이 원소를 고르는 경우의 수**
### 1. 조합의 핵심 개념
- 순서가 없음: 뽑는 순서가 달라도 구성원이 같으면 같은 경우로 봅니다 (예: 대표 2명을 뽑을 때 'A, B'를 뽑는 것과 'B, A'를 뽑는 것은 같습니다).
- 기호 표기: 서로 다른 n개 중 r개를 택하는 조합은 \({}_{n}C_{r}\) 또는 \({n \choose r}\)로 나타냅니다.
- 계산 공식:\(_nC_r = \frac{_nP_r}{r!} = \frac{n!}{r!(n-r)!}\)(순열 \({}_{n}P_{r}\)을 구한 뒤, 순서가 생겨서 중복된 만큼(r!) 나누어 줍니다.)
