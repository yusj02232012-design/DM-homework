# 1. 분산 처리 문제 코드
```
T = int(input())
for i in range(T):
    a, b = map(int, input().split())

    print(str(a**b)[-1] if str(a**b)[-1] != "0" else 10)
```
# 2. 나머지 연산
- 의미 : 어떤 수를 다른 수로 나눈 뒤 몫이 아닌 남은 값(나머지)을 구하는 산술 및 프로그래밍 연산
## 기본 개념기호 및 연산자
- 대부분의 프로그래밍 언어(C, Java, Python, JavaScript 등)에서는 퍼센트 기호인 %를 나머지 연산자로 사용합니다.
- 예시: 7 % 3의 결과는 1입니다. (7을 3으로 나누면 몫이 2이고 나머지가 1이기 때문입니다.)
- 나누어떨어질 때: 나머지가 0이면 앞의 숫자는 뒤의 숫자로 정확하게 나누어떨어집니다
## 주요 활용처
- 짝수와 홀수 구분: 어떤 수든 number % 2의 결과가 0이면 짝수, 1이면 홀수입니다.
- 배수 확인: number % n == 0을 통해 특정 수의 배수인지 쉽게 판별할 수 있습니다.
- 주기적인 범위 제한: 게임이나 시계 바늘 등 일정한 범위(예: 0~59분) 안에서 값을 순환시킬 때 유용합니다
# 3. 주기성
- 의미: 어떠한 상태 등이 일정한 시간적 간격을 두고 반복하여 나타나는 성질을 뜻한다.
